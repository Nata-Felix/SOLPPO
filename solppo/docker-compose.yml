version: '3.8'

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: solppo-web
    container_name: solppo-web
    restart: always
    ports:
      - "3000:3000"
    environment:
      - DB_PATH=/app/data/solppo.db
    volumes:
      - ./solppo-data:/app/data
